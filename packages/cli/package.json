{
  "name": "@distill/cli",
  "version": "0.1.0",
  "type": "module",
  "description": "CLI for LLM agent migration: profile, migrate, evaluate",
  "bin": {
    "distill": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "start": "node dist/index.js",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@distill/core": "workspace:*",
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
